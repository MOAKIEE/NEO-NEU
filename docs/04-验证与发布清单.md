# 验证与发布清单

更新日期：2026-09-23。本清单区分代码构建、已有数据层实测与仍需在正式 App 上验证的场景。详细接口样本边界见[数据层交接](handoff/data.md)和[接口研究](03-接口与认证研究.md)。

## 本轮界面与动效重构

分支 `feature/ui-refresh-miuix`。重建设计令牌（CampusColors/CampusMotion/CampusShapes/CampusSpacing）、通用组件库（CampusCard/CampusGroup/CampusRow/CampusFilterChip/CampusSegmentedControl/AnimatedNumber/LoadStatePanel 等）、主框架转场与全部页面的视觉与动效。数据契约、查询端点、Room schema 与页面行为未变更，详见[前端说明](handoff/frontend.md)与[界面设计系统](05-界面设计系统与动效.md)。

- `testDebugUnitTest :app:assembleDebug` 最终结果为 BUILD SUCCESSFUL；`core/ui` 既有 10 项课程逻辑测试全部通过，Debug APK 可生成。
- `app:testDebugUnitTest` 为 NO-SOURCE。

### 模拟器实拍核验（已做）

设备：AVD `TestDevice`，Android 36.1 / google_apis_playstore / x86_64 / 1080×2400 / 440dpi / swiftshader_indirect。安装 Debug APK 后逐页截图核对；截图不随仓库保存，取图与复现方式见[界面设计系统](05-界面设计系统与动效.md#界面核验方式)。

| 已核对 | 结果 |
| --- | --- |
| 冷启动与四栏切换 | 无崩溃，`logcat` 无 FATAL；今日／课表／查询／我的均正常渲染 |
| 浅色与深色 | 两种模式可切换并持久化；深色页面底色 `#0F131B`、卡片 `#181E2A`，正文与次要文字对比度可读 |
| 顶部安全区 | 大标题起点在状态栏 128px 之下；已在模拟器上发现并修复 `Scaffold.statusBarsPadding()` 与 Miuix 系统栏内边距重复导致的 128px 空白 |
| 标题栏折叠 | 二级页面滚动后大标题收起为小标题，返回箭头常驻 |
| 底色一致性 | 已修复标题栏与页面底色断层、课表白色表体直边、底部信息条白色色带三处；截图复验通过 |
| 二级页面入场 | 从查询页进入成绩查询为右侧滑入，返回手势正常 |
| 按钮、标签、开关与分段控件 | 外观、按压反馈与选中态正常；深色下主按钮与文字对比正常 |

### 仍未在设备上核对

- 登录后的真实数据页面：课表网格课程块、成绩列表与详情、余额数字滚动、消息与待办列表、考试条目。当前只有登录前的空状态与错误状态截图。
- 大字体（1.3／2 倍）、320dp 窄屏、横屏与进程重建。
- TalkBack 语义、后台预览隐私、手势导航与键盘避让。
- release 构建的滚动与切页流畅度（本轮使用 Debug + 软件渲染 GPU，帧率不代表真机表现）。
- 点击反馈、骨架屏与交错入场只能从静态截图推断，未做逐帧录像核对。

## 上一轮 V2 重构审查修复

审查基线 `81a7b53`。该轮修复硬编码作息/校历、首页错周与时间判断、跨节冲突、无来源账户状态、清理操作文案、页面状态保存、考试详情定位及消息/待办加载；正式 App 已移除演示模式。

- 新增 core/ui 课程逻辑测试 10 项，全部通过，0 失败/0 错误；它们验证确定性业务判断，不验证 Compose 渲染或真实学校字段。
- `app:testDebugUnitTest` 为 NO-SOURCE。既有数据测试中 UP-TO-DATE 代表复用检查结果，不是再次真实登录验证。

## 已有证据

| 范围 | 当前记录 |
| --- | --- |
| 工程 | `testDebugUnitTest :app:assembleDebug` 在本地通过；正式 Debug APK 可生成。 |
| 数据层模拟器 | 官方网页登录和双域会话探针、学期／周次／课表、一个历史学期的成绩和详情、校园卡与网费余额、消息，以及当前账号的空考试／空待办已实测。 |
| 缓存与失效 | Room 迁移、进程重启、离线失败保留旧课表、认证失效保留旧课表及恢复曾在独立验证宿主中通过。 |
| UI 实现 | 四个底部入口及全部查询页面已完成设计令牌、组件与动效重构（见[界面设计系统](05-界面设计系统与动效.md)），并在 Android 36.1 模拟器上完成登录前状态的逐页实拍核验；上一轮修复了数据可信度、课程状态、导航保存、弹层及列表查询问题。登录后的真实数据页、大字体、横屏与 release 性能仍未验收。 |

## 待补真实样本

- **考试**：目前仅有空列表样本。获得本人非空考试时核对时间、地点、座位、状态和详情；不以演示数据宣称通过。
- **待办与申请**：目前仅有空列表样本。非空记录会被适配器标为 `SCHEMA_CHANGED`；取得脱敏结构后再更新解析与 UI，并验证分页。
- **课表边界**：多校区、非空实践课程、单双周和临时地点变化尚未核对；节次起止时间与组合教师／周次描述也需验证。
- **成绩与消息**：历史成绩仅核对过一个学期；消息翻页、来源和已读筛选的其他组合仍需实际样本。
- **设备环境**：WebVPN／校外路径、跨账号切换、API 24 真机与不同系统 WebView 尚未验收。

## 正式 App 验收

- [ ] 全新安装后由用户在官方页面登录；返回原生页面并完成门户、教务双域复验。
- [ ] 在正式 `app/` 中逐页核对课表、成绩、考试、余额、消息和待办的真实数据、空状态、失败状态与更新时间；与学校页面逐项比对可见值。
- [ ] 杀进程重开、断网、认证过期后确认旧缓存只在当前账号作用域显示，恢复时正确刷新；退出后清理会话和当前作用域缓存。
- [ ] 切标签、进入详情和返回后保留筛选与滚动；复验旋转、进程恢复、Miuix 抽屉及确认框的返回行为。
- [ ] 320dp/390dp、字体 1.3/2 倍、TalkBack、后台预览隐私、手势导航、键盘安全区及长列表／课表切周在目标真机上检查。模拟器已覆盖 393dp 与深色模式，其余仍待验收。
- [ ] 验证课程跨日、跨周、真实冲突及多校区列表；确认缺时间、过期数据不会生成确定性的课程状态。
- [ ] 使用 release 构建检查滑动、刷新和切页；确认网络及解析不阻塞主线程。
- [ ] 确认所有核心查询留在原生页面，学校网页只用于认证或用户主动打开原文；没有支付、选课、挂失、申请提交、课表确认、承诺书确认或服务端已读写请求。
- [ ] 发布前核对应用标识、签名、版本、隐私文案、第三方许可证与目标 Android 版本行为。

运行现有自动检查：

```powershell
.\gradlew.bat testDebugUnitTest :app:assembleDebug
```

### 复现模拟器核验

```powershell
$sdk = "$env:LOCALAPPDATA\Android\Sdk"
& "$sdk\emulator\emulator.exe" -avd TestDevice -gpu swiftshader_indirect -no-boot-anim
& "$sdk\platform-tools\adb.exe" install -r app\build\outputs\apk\debug\app-debug.apk
& "$sdk\platform-tools\adb.exe" shell am start -n edu.neu.campus.neoneu/edu.neu.campus.app.MainActivity
& "$sdk\platform-tools\adb.exe" exec-out screencap -p > shot.png
```

用 `adb shell uiautomator dump /sdcard/ui.xml` 取控件实际坐标，比按截图估算点击位置可靠；查控件文本前先确认前台包名，避免把其他应用的前台界面误当成被测应用。

`verification/` 可用于继续采集**脱敏的字段结构**，不可把姓名、学号、成绩值、余额、密码、Cookie、票据或原始 HAR 提交到仓库。正式 App 的真机验收应单独记录结果，不以此处历史验证宿主记录替代。
