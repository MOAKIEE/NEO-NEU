define(function (require, exports, module) {
    var config = {
        /*
             APP标题
         */
        APP_TITLE: "课表查询",
        /*
            开启页面flex布局 驱使表格autoheight
         */
        LAYOUT_FLEX: true
    };

    return config;
});
